# Dependencies needed by every PXA System product, independent of board IO.
# Board-only packages belong in firmware/boards/<board>/idf_component.yml.
dependencies:
  idf:
    version: ">=5.5.4"
  lvgl/lvgl: "^9.6.0~1"
  espressif/esp_lvgl_port: "~2.9.0"
  joltwallet/littlefs: "^1.22.3"
